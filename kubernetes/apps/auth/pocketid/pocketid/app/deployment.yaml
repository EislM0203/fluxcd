apiVersion: apps/v1
kind: Deployment
metadata:
  name: pocket-id
  labels:
    app: pocketid
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: pocketid
  template:
    metadata:
      labels:
        app: pocketid
    spec:
      containers:
        - name: pocket-id
          image: harbor.cloud.traunseenet.com/ghcr.io/pocket-id/pocket-id:v2.13@sha256:c9c1d7b70006968d673a1073df048a3fdec73ccb54e182320b56bb911340f6f1
          env:
            - name: APP_URL
              value: "https://pocketid.cloud.traunseenet.com"
            - name: TRUST_PROXY
              value: "true"
            - name: ENCRYPTION_KEY                                                                                                           
              valueFrom:                                      
                secretKeyRef:                                                                                                                
                  name: pocketid-secret
                  key: ENCRYPTION_KEY 
          ports:
            - containerPort: 1411
              name: http
              protocol: TCP
          volumeMounts:
            - name: pocketid-data
              mountPath: /app/data
          livenessProbe:
            exec:
              command:
                - /app/pocket-id
                - healthcheck
            initialDelaySeconds: 10
            periodSeconds: 90
            timeoutSeconds: 5
            failureThreshold: 2
          readinessProbe:
            exec:
              command:
                - /app/pocket-id
                - healthcheck
            initialDelaySeconds: 10
            periodSeconds: 90
            timeoutSeconds: 5
            failureThreshold: 2
      volumes:
        - name: pocketid-data
          persistentVolumeClaim:
            claimName: pocketid-data
