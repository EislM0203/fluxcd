apiVersion: v1
kind: Service
metadata:
  name: pocket-id
  labels:
    app: pocket-id
spec:
  type: ClusterIP
  selector:
    app: pocket-id
  ports:
    - name: http
      protocol: TCP
      port: 1411
      targetPort: 1411

