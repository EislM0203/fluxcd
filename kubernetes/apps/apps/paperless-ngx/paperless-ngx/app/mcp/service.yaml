apiVersion: v1
kind: Service
metadata:
  name: paperless-mcp
  labels:
    app: paperless-mcp
spec:
  type: ClusterIP
  selector:
    app: paperless-mcp
  ports:
    - name: http
      port: 5000
      targetPort: http
      protocol: TCP
