---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: actual-budget
  name: actual-budget-data
  annotations:
    numberOfReplicas: '1'
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: longhorn-storage
  # Fresh install: no restic snapshot exists yet, so the PVC starts empty.
  # To RESTORE from a volsync backup later, uncomment the dataSourceRef below
  # together with the ReplicationDestination block, then reconcile.
  # dataSourceRef:
  #   kind: ReplicationDestination
  #   apiGroup: volsync.backube
  #   name: actual-budget-restore
  resources:
    requests:
      storage: 2Gi
# ---
# # yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/volsync.backube/replicationdestination_v1alpha1.json
# apiVersion: volsync.backube/v1alpha1
# kind: ReplicationDestination
# metadata:
#   name: actual-budget-restore
# spec:
#   trigger:
#     manual: restore-once
#   restic:
#     repository: restic-actual-budget
#     storageClassName: longhorn-storage
#     accessModes: ["ReadWriteOnce"]
#     copyMethod: Snapshot
#     capacity: 2Gi
#     enableFileDeletion: false
#     cleanupCachePVC: false
#     cleanupTempPVC: false
