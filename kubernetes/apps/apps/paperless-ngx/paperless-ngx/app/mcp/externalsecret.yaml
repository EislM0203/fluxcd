---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: paperless-mcp
spec:
  refreshInterval: 5m
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: paperless-mcp
  dataFrom:
    - extract:
        key: apps/paperless-mcp

