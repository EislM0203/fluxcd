apiVersion: v1
kind: Service
metadata:
  name: github-mcp
  labels:
    app: github-mcp
spec:
  type: ClusterIP
  selector:
    app: github-mcp
  ports:
    - name: http
      port: 8082
      targetPort: http
      protocol: TCP
