apiVersion: v1
kind: Service
metadata:
  name: karakeep-mcp
  labels:
    app: karakeep-mcp
spec:
  type: ClusterIP
  selector:
    app: karakeep-mcp
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
