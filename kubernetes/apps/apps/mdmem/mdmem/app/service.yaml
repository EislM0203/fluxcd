---
apiVersion: v1
kind: Service
metadata:
  name: mdmem
  labels:
    app: mdmem
spec:
  selector:
    app: mdmem
  ports:
    - name: mcp
      port: 8080
      targetPort: mcp
    - name: ui
      port: 8081
      targetPort: ui
