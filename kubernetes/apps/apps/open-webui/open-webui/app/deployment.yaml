apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: openwebui
  name: openwebui
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app: openwebui
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: openwebui
    spec:
      containers:
        - image: ghcr.io/open-webui/open-webui:0.8.5
          imagePullPolicy: IfNotPresent
          name: open-webui
          ports:
            - containerPort: 8080
              name: webui
              protocol: TCP
          envFrom:
            - configMapRef:
                name: open-webui-env
            - secretRef:
                name: open-webui-secret
          volumeMounts:
            - mountPath: /app/backend/data
              name: data
      # dnsPolicy: ClusterFirst
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: openwebui-data