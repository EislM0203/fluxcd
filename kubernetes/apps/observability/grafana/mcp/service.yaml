apiVersion: v1
kind: Service
metadata:
  name: grafana-mcp
  labels:
    app: grafana-mcp
spec:
  type: ClusterIP
  selector:
    app: grafana-mcp
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
